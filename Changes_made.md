Config.js
-Changed the link in the Config.js file and added my Neon account link

.gitignore
-Moved the file to outside inside the folder in order for it to have only one
-Moved everything inside the seperate .gitignores and combined them
